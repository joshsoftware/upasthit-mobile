<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppThemeDark.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="TransparentToolbar" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:windowActionBarOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
    </style>

    <!--Toolbar Style-->
    <style name="toolbar_title">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>


    <style name="TextLabel" parent="Widget.Design.TextInputLayout">
        <!-- Hint color and label color in FALSE state -->
        <!--<item name="android:textColorHint">@color/colorPrimary</item>-->
        <item name="android:textSize">18sp</item>
        <!--<item name="android:editTextColor">@color/colorPrimary</item>-->
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <!--<item name="colorAccent">@color/colorPrimary</item>-->
        <!--<item name="colorControlNormal">@color/colorPrimary</item>-->
        <item name="colorControlActivated">@color/colorPrimary</item>
    </style>
</resources>
